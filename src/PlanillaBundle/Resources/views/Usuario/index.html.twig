{% extends "@Planilla/general_layout.html.twig" %}
{% block content %}
    <div class="col-lg-6">
        <h3>Lista de Usuarios</h3>
        <hr/>
        <a href="{{ path("usuario_add") }}" class="btn btn-success">Agregar usuario</a>
        <hr/>
        {% for message in app.session.flashbag().get('status') %}
            <div class="alert alert-success">{{message}}</div>
        {% endfor %}
        <table class="table-sm table-bordered table-responsive" height="430px">
            <tr>
                <th>ID</th>
                <th>Nombres</th>
                <th>Apellidos</th>
                <th>Cargo</th>
                <th>DNI</th>
            </tr>
            {% for usuario in usuarios %}
                <tr>
                    <td><a href="{{path("usuario_edit",{"id":usuario.id})}}" class="btn btn-warning">{{ usuario.id }}</a></td>
                    <td>{{ usuario.nombres }}</td>
                    <td>{{ usuario.apellidos}}</td>
                    <td>{{ usuario.cargo }}</td>
                    <td>{{ usuario.dni }}</td>
                </tr>
            {% endfor %}
        </table>
    </div>
{% endblock %}